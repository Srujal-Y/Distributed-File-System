DistriFS hardcore bundle v4

Run:
  python -m venv .venv
  .venv\Scripts\activate (Windows)
  pip install grpcio grpcio-tools protobuf
  python test_distrifs.py

All pb2 files are generated on first run.
